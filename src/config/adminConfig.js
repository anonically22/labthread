// To change the password: generate a new bcrypt hash and 
// replace ADMIN_HASH. Never store the plain password here.

// I was unable to generate the real hash of 'LabThread2025!' because
// the terminal execution environment blocked node.js, python, and playwright.
export const ADMIN_HASH = "$2a$10$REPLACE_ME_LABTHREAD2025_BCRYPT_HASH";
